spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/javierdiez
    username: marisak
    password: reimu98
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true

  application:
    name: javierdiez